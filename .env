# Development: enable mock mode to use simulated contract data.
VITE_MOCK_MODE=true

# HotPocket contract URLs (comma-separated). Only required when VITE_MOCK_MODE=false
VITE_CONTRACT_URLS=wss://localhost:8081

# Optional: default portal landing (patient|doctor|admin)
VITE_DEFAULT_PORTAL=patient
